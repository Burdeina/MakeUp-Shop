<!DOCTYPE html>
<html>
<head>
  <% include blocks/links.ejs %>
  <link rel="stylesheet" href="/css/product.css">
  <title> <%= title %> </title>
  <script src="https://code.jquery.com/jquery-3.1.1.min.js"></script>
</head>
<body>
  <% include blocks/menu.ejs %>
  <div id="main">
    <% include blocks/register_menu.ejs %>
        <% include messaging_layout.ejs %>
  <div class="container">
  <div style="text-align:center">
    <h2><%= product.name %></h2>
    <p>Swing by for a cup of coffee, or leave us a message:</p>

  </div>
  <div class="row" style="width: 90%">
    <div class="column">
      <img src="/images/<%= product.name %>.jpg" alt="<%= product.name %>" style="width:100%;">
    </div>
    <div class="column">
      <% if(!user); else {%>
          <% if(user.isAdmin) {%>
            <h2>ID: <%= product._id %></h2>
          <%}%>
      <%}%>
    <h2>Producer: <%= product.producer %></h2>
    <h2>Volume: <%= product.volume %></h2>
    <h2>Classification: <%= product.classification %></h2>
    <h2>Made in: <%= product.made_in %></h2>
    <h2>Price: <%= product.price - (product.discount * product.price/100) %>&#36;</h2>
    <% if(product.discount != 0) {%>
      <h2 style="color: red;">Discount: <%= product.discount %>&#37;</h2>
      <% } %>
        <p><a href="/products/add_to_cart/<%= product._id %>/<%= product.name %>/<%= product.price - (product.discount * product.price/100) %>" target="_parent"><button>Buy</button></a></p>
        <% if(user) {%>
          <% if(user.isAdmin == true) {%>
            <p><a href="/products/edit/<%= product._id %>" target="_parent"><button>Edit</button></a></p>
            <p><a href="/products/add_discount/<%= product._id %>" target="_parent"><button>Add discount</button></a></p>
            <a class = "delete-product" href="#" id="blabla" data-id=<%= product._id %> teemp="12s4f"><button class="delete">Delete</button></a>
          <%}%>
        <%}%>
    </div>
  </div>



</div>
<% include blocks/footer.ejs %>

</div>



<script src="/scripts/menuScript.js"></script>
<% include blocks/script_links.ejs %>
<script src="https://code.jquery.com/jquery-3.1.1.min.js"></script>
<script src="/scripts/delete.js"></script>
</body>
</html>
